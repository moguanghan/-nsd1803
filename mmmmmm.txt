算术运算：
整数：expr  $[]  let
小数：bc  scale=4 （小数点位数）
判断：
字符串判断 [ == ] [ != ] [  -z ]  [  !-z  ]
数字 -eq -ne -gt -ge -lt -le
文件或者目录是否存在 [ -e  木录或者文件 ] 存在才为真
文件是否存在  存在才为真 
目录是否存在  存在才为真
对象是否可读  可读才为真
对象是否可写  可写才为真
对象是否可执行  可执行才为真
函数： 函数名 () {
定义函数
  }
case 变量  in
值列表)
	命令 ；；
值列表)
	命令 ；；
*）
	命令
esac
循环 （for。while）while：直接死循环  while  条件  （条件为真一直执行）
中断 continiue  break exit 
